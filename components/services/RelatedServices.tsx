import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { getServiceBySlug, getServicePath, type ServiceContent } from "../../lib/content/services";
export function RelatedServices({ service }: { service: ServiceContent }) { return <section className="bg-slate-100 px-4 py-20 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><h2 className="text-4xl font-black">Related auto electrical services</h2><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{service.relatedServiceSlugs.map(slug => { const related = getServiceBySlug(slug); return related && <Link key={slug} href={getServicePath(slug)} className="flex items-center justify-between rounded-lg border border-slate-200 bg-white p-5 font-black shadow-md transition hover:border-asap-cyan hover:text-asap-purple hover:shadow-lg focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-asap-cyan/35">{related.shortTitle}<ArrowRight size={18} aria-hidden /></Link>; })}</div></div></section>; }
